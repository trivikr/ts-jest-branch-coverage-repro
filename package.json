{
  "name": "ts-jest-branch-coverage-repro",
  "version": "1.0.0",
  "description": "Repro repo for branch coverage issue with ts-jest@20.0.x",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "test": "jest --coverage --no-cache --config ./jest.json"
  },
  "keywords": ["Jest", "TypeScript", "branch", "coverage"],
  "author": "Trivikram Kamat",
  "license": "ISC",
  "dependencies": {
    "handlebars": "^4.0.10"
  },
  "devDependencies": {
    "@types/handlebars": "^4.0.35",
    "@types/jest": "^20.0.7",
    "jest": "^20.0.4",
    "ts-jest": "^20.0.10",
    "typescript": "^2.4.2"
  }
}
